# dir-size
dotnet global tool to list dir with size.

# Install

```powershell
> dotnet tool install --global dir-size
```

## Usage:

```powershell
> dir-size
```

```powershell
> dir-size -d path/to/dir
```
